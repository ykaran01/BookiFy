// import {requireAdmin, requireAuth } from "../middleware/Authentication.js";
// import { reviewController,getProductReviews, deleteReview} from "../controllers/review.contoller.js";
import { Router } from "express";

const review = Router()

// review.use(requireDbAuth)
// review.post('/review/:productId',reviewController)
// review.get('/getreview/:productId',getProductReviews)
// review.get('/deletereview/:reviewId', deleteReview)
export default review

